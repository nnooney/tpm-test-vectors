// If the Object is a not a keyedHash object, and the sign and encrypt
// attributes are CLEAR, the TPM shall return TPM_RC_ATTRIBUTES.
TpmTestVector(
  name: "[TPM2_Load] keyedHash sign not consistent",
  spec_versions: [
    184,
  ],
  test_sequence: [
  ]
)
