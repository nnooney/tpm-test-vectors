// If keyHandle is not a Storage Key, then the TPM shall return TPM_RC_TYPE.
TpmTestVector(
  name: "[TPM2_ActivateCredential] keyHandle is not a storage key",
  spec_versions: [
    184,
  ],
  test_sequence: [
  ]
)
