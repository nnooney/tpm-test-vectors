// Authorization for activateHandle requires the ADMIN role.
TpmTestVector(
  name: "[TPM2_ActivateCredential] activateHandle and admin role",
  spec_versions: [
    184,
  ],
  test_sequence: [
  ]
)
