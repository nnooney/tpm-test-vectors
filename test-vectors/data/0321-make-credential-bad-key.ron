// The loaded public area referenced by handle is required to be the public area
// of a Storage key, otherwise, the credential cannot be properly sealed.
TpmTestVector(
  name: "[TPM2_MakeCredential] non strage key used",
  spec_versions: [
    184,
  ],
  test_sequence: [
  ]
)
