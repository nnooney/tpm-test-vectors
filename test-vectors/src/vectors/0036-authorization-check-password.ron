// If the authorization uses a password, then the password matches the authValue
// associated with the handle (TPM_RC_AUTH_FAIL or TPM_RC_BAD_AUTH).
TpmTestVector(
  name: "[Authorization Checks] Password session authValue for handle",
  spec_versions: [
    184,
  ],
  test_sequence: [
  ]
)
